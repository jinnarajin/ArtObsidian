# ArtObsidian

미술 자료용 가벼운 위키. **SwiftUI 멀티플랫폼 앱(iOS + macOS)** 으로 개발 중.
이전 웹 버전(단일 HTML + server.py)은 폐기 — git 히스토리(`8733251` 이전)에만 존재.

## 절대 규칙
- SwiftUI 멀티플랫폼 하나의 코드베이스로 iOS와 macOS 둘 다 지원
- 노트는 평문 `.md` 파일로 저장 — 전용 DB 포맷 금지, 옵시디언 볼트와 호환되게
- 외부 패키지는 꼭 필요할 때만 (표준 프레임워크 우선)

## 핵심 기능 (웹 버전에서 이어받음)
- 마크다운 편집 / 읽기 모드
- `[[위키링크]]` + 자동완성
- 폴더 트리, 검색, 태그(#), Spotlight 스타일 빠른 검색(Cmd+K)
- 그래프 뷰 (위키링크 = 엣지)
- 유사도 기반 관련 노트

## AI 에이전트 팀 구성 (오케스트레이션)

사용자가 지휘자. 각 팀은 서브에이전트/병렬 세션으로 굴리고, 산출물은 아래 위치에 마크다운으로 남긴다.

| 팀 | 담당 | 산출물 위치 |
|---|---|---|
| 리서치/기획팀 | 시장조사, 경쟁사 분석, 사용자 페인포인트, 문제-솔루션-타겟, 기능 우선순위, 로드맵 | `docs/research/`, `docs/prd/` |
| 디자인팀 | 트렌드 리서치, 디자인 제안, 브리프, 에셋 정리 | `design/` |
| 개발팀 | SwiftUI 구현, 스펙 문서, 기술 검토 | `ArtObsidian/`, `docs/specs/` |
| 검증팀 | 피처별 QA 체크리스트, 합격 기준, 검증 프로세스, 피드백 | `qa/` |

### 팀 공통 규칙
- 모든 문서는 마크다운, 상단에 날짜(절대 날짜)와 버전 명시
- 개발은 `docs/prd/` 최신 기획서 기준으로만 진행 — 기획서에 없는 기능 임의 추가 금지
- 검증팀은 기능별 합격 기준을 `qa/checklist.md`에 **먼저** 정의한 후 검증
- 레퍼런스 링크는 `references.md`에 링크 + 한 줄 요약으로 누적
- 병렬 작업은 브랜치(`research/*`, `design/*`, `dev/*`, `qa/*`)로 진행, 중간 보고 = PR, 사용자 리뷰 후 main 머지

## 구조
- `ArtObsidian/` — SwiftUI 소스 (Xcode 멀티플랫폼 프로젝트)
- `vault/` — 테스트용 샘플 노트
- `docs/decisions.md` — 결정 기록
- `docs/research/`, `docs/prd/`, `docs/specs/` — 기획·스펙 문서
- `design/` — 디자인 산출물 (트렌드, 브리프, `assets/`)
- `qa/` — 검증 문서 (checklist.md, process.md)
- `references.md` — 레퍼런스 링크 모음

## 맥락
제작자는 옵시디언으로 PARA 볼트를 운영 중. 이 앱은 그림 자료를 iPad/맥에서 보며 쓰는 개인 위키. 배경 노트는 옵시디언 볼트 `Resource/미술옵시디언/`.

## 작업 후
- 결정하거나 방향 바꾼 게 있으면 docs/decisions.md에 3~4줄로 추가할 것 (날짜 / 결정 / 이유 / 버린 대안).
- 작업 세션이 끝나면 `docs/ai-feedback/TEMPLATE.md`를 복사해 `YYYY-MM-DD-이름-주제.md`로 회고 로그 작성. 사용자가 요청하면 Claude가 세션 기록을 바탕으로 0~4번 항목(사용 도구, 프롬프트 원문, 시행착오)을 초안으로 채워줄 것 — 5번 회고는 사용자 본인이 작성.
